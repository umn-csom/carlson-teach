# Libraries directory

This directory should hold any external javacript or php libraries needed for your site. Any modules
requiring additional libraries will instruct you to keep the files in `sites/all/libraries` or 
`sites/default/libraries`, and in a multisite installation it is fine to put them in a site-specific
libraries directory

